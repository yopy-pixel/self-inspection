<?xml version="1.0" encoding="utf-8"?>
<!--
  デバッグビルド専用のネットワーク設定。

  **リリースには含まれない**（src/debug に置いてあるため）。
  ローカルの開発サーバー（server/dev-server.ts）へ繋ぐため、
  以下のホストに限って平文 HTTP を許可する。

    10.0.2.2   … Android エミュレータから見たホストマシン
    localhost  … 端末内（adb reverse を使う場合）

  これらは端末内・LAN 内で完結し、外部には出ない。
  本番の Cloudflare（https）は base-config の既定に従う。
-->
<network-security-config>
    <!-- 既定は本番と同じく平文禁止 -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- 開発サーバーへの接続だけ例外 -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
